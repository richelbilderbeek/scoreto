#' Extract the course names from an SLUBI courses text
#' @param course_names subset of raw HTML from the SLUBI website.
#' @export
extract_slubi_course_names <- function(course_names) {
  stop("Deprecated")
}
